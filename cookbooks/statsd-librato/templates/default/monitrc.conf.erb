check process statsd
  with pidfile /data/<%= @app_name %>/shared/pids/statsd.pid
  start = "/data/<%= @app_name %>/shared/statsd start" as uid deploy and gid deploy
  stop = "/data/<%= @app_name %>/shared/statsd stop" as uid deploy and gid deploy
  group statsd
